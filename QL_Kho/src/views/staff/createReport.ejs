<%- include('../partials/staff/header') %>
<!-- <link rel="stylesheet" href="/css/staff.css">
    
</head>

<body>
    <header class="admin-header">
        <div class="logo">Warehouse Management System - Staff</div>


        <nav class="admin-nav">
            <ul>
                <li><a href="/staff/dashboard">Dashboard</a></li>
                <li><a href="/staff/reports">My report</a></li>
                <li><a href="/staff/reports/import">Import goods</a></li>
                <li><a href="/staff/reports/export">Export goods</a></li>
                <li><a href="/staff/change-password">Change password</a></li>
                <li><a href="/logout">Log out</a></li>
            </ul>
        </nav>
    </header> -->
    <!-- <link rel="stylesheet" href="/css/staff/changePassword.css"> -->

    <div class="container">
        <h1>Create a report <%= reportType==='import' ? 'NHẬP' : 'XUẤT' %> goods</h1>

        <% if (error) { %>
            <div class="alert alert-danger">
                <%= error %>
            </div>
            <% } %>

                <form method="POST" action="/staff/reports/<%= reportType %>">
                    <div class="form-group">
                        <label for="product_id">Product *</label>
                        <select class="form-control" id="product_id" name="product_id" required>
                            <% products.forEach(product=> { %>
                                <option value="<%= product.id %>">
                                    <%= product.name %> (Tồn: <%= product.quantity %>, Giá: <%=
                                                product.price.toLocaleString() %> VNĐ)
                                </option>
                                <% }) %>
                        </select>
                    </div>

                    <div class="form-group">
                        <label for="quantity">Quantity *</label>
                        <input type="number" class="form-control" id="quantity" name="quantity" min="1" required>
                    </div>

                    <div class="form-group">
                        <label for="notes">Note</label>
                        <textarea class="form-control" id="notes" name="notes" rows="3"></textarea>
                    </div>

                    <button type="submit" class="btn btn-primary">Send the report</button>
                    <a href="/staff/reports" class="btn btn-secondary">Cancel</a>
                </form>
    </div>
    
